# Daily Digest Maker Configuration

# Slack Configuration (optional)
# Get your bot token from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Gmail Configuration (optional)
# Download credentials from Google Cloud Console
GMAIL_CREDENTIALS_PATH=credentials/gmail_credentials.json
GMAIL_TOKEN_PATH=credentials/gmail_token.json

# SMTP Configuration (required)
# For Gmail: use smtp.gmail.com with port 587
# For Outlook: use smtp-mail.outlook.com with port 587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME
SMTP_PASSWORD=
SMTP_USE_TLS=true

# Recipient Configuration (required)
RECIPIENT_EMAIL=

# Schedule Configuration (optional)
SCHEDULE_TIME=20:00

# Logging Configuration (optional)
LOG_LEVEL=INFO

# WhatsApp Configuration (optional)
# Set to true to include placeholder WhatsApp messages (for testing)
ENABLE_WHATSAPP_PLACEHOLDER=false
